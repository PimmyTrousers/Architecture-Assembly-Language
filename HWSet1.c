#include <stdio.h>
#define AREA(h,b) (((h)*(b))/2)

int main(void){
	/*double a, b, h, c, y, z;
	a = 1;
	b = 2; 
	h = 3; 
	c = 4;

	y = ((a*a)-(4*a*c))/(2*a);
	printf("The value of y is %lf\n", y);
	z = AREA(2,3);
	printf("The value of z is %lf\n", z);*/

	int i = 10;
	switch(i){
		case 1:
			printf("one");
			break;
		case 2:
			printf("two");
			break;
		defualt:
			printf("NONE");
	}
}